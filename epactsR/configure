#!/bin/bash

echo 'PKG_CXXFLAGS=--std=c++14 -pthread' > src/Makevars
echo "PKG_CPPFLAGS=-I${INCLUDE_DIR}" >> src/Makevars
echo "PKG_LIBS=-L${LINK_DIR} -lz -lsavvy -lhts -lzstd -pthread" >> src/Makevars